{
  "name": "dsh-plugin-builder",
  "version": "0.1.0",
  "description": "A guided DSH Agent preset for building verified, installable plugins.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "dsh-plugin-builder": "./src/cli.mjs"
  },
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "files": [
    "src/",
    "preset/",
    "docs/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test --test-isolation=none tests/*.test.mjs"
  }
}
